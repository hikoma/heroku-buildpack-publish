#!/usr/bin/env bash

indent() {
  sed -u 's/^/       /'
}

echo "-----> Found a .heroku/app/Procfile"

cd $1

rm -rf !(.heroku|.profile.d) | indent

rsync -av .heroku/app . | indent

rm -rf .heroku/app | indent
